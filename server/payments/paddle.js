// Paddle integration sample (placeholder)
// Paddle provides checkout links and webhooks for subscription billing.

async function createCheckout({ productId, email }) {
  // Placeholder: call Paddle API or construct checkout URL
  return { status: 'not-implemented', message: 'Paddle integration requires vendor credentials' };
}

module.exports = { createCheckout };
